# ONDE — baseline ast-grep (mise à jour 2026-08-23 après T30)
# Outil : ast-grep 0.45.1 · config : .ast-grep/sgconfig.yml · commande : ast-grep scan -c .ast-grep/sgconfig.yml
# Sémantique gate : tout finding ABSENT de ce fichier = finding de checker (error => bloquant sauf justification ADR/expect documenté).

core/src/storage/mod.rs:78:48: note[onde-rust-expect-critical]: expect() hors tests dans un module critique — vérifier que le message documente l'invariant non tenu (info checker)
core/src/storage/mod.rs:267:21: note[onde-rust-expect-critical]: expect() hors tests dans un module critique — vérifier que le message documente l'invariant non tenu (info checker)
core/src/storage/mod.rs:300:5: note[onde-rust-expect-critical]: expect() hors tests dans un module critique — vérifier que le message documente l'invariant non tenu (info checker)
core/src/storage/mod.rs:596:9: note[onde-rust-expect-critical]: expect() hors tests dans un module critique — vérifier que le message documente l'invariant non tenu (info checker)
core/src/storage/mod.rs:599:9: note[onde-rust-expect-critical]: expect() hors tests dans un module critique — vérifier que le message documente l'invariant non tenu (info checker)
core/src/crypto/mod.rs:88:9: note[onde-rust-expect-critical]: expect() hors tests dans un module critique — vérifier que le message documente l'invariant non tenu (info checker)
core/src/crypto/mod.rs:749:24: note[onde-rust-expect-critical]: expect() hors tests dans un module critique — vérifier que le message documente l'invariant non tenu (info checker)
core/src/protocol/mod.rs:218:20: note[onde-rust-expect-critical]: expect() hors tests dans un module critique — vérifier que le message documente l'invariant non tenu (info checker)
