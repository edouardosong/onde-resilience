<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- AndroidTV support -->
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <!-- Permission signature-level réservée au FileProvider : seuls les
         composants signés avec la clé applicative ONDE peuvent y accéder. -->
    <permission
        android:name="citoyen.onde.mesh.permission.FILE_PROVIDER"
        android:protectionLevel="signature" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.onde_ui"
        android:usesCleartextTraffic="${usesCleartextTraffic}">
        <!-- exported="true" est requis pour l'activité lanceur
             (MAIN/LAUNCHER + LEANBACK_LAUNCHER). -->
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:label="@string/main_activity_title"
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- AndroidTV support -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- FileProvider : exported="false" + permission signature-level
             + grantUriPermissions — accès au partage de fichiers strictement
             limité (correctif "content provider should specify precise
             permissions"). -->
        <provider
          android:name="androidx.core.content.FileProvider"
          android:authorities="${applicationId}.fileprovider"
          android:exported="false"
          android:grantUriPermissions="true"
          android:readPermission="citoyen.onde.mesh.permission.FILE_PROVIDER"
          android:writePermission="citoyen.onde.mesh.permission.FILE_PROVIDER">
          <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>